# Example arrays
a = [1, 2, 3, 4, 5]
b = [1, 2, 3]

# Convert to sets and take the union
union_set = set(a) | set(b)

# Count number of unique elements
print("Number of elements in union:", len(union_set))